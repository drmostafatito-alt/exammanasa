# FINAL TEACHER PLATFORM AUDIT — ExamManasa

**التاريخ:** 2026-09-11 · **الفرع المُدمَج:** `main` (بعد PR #9 `ef50b13`) · **المُدقِّق:** وكيل المراجعة النهائية المستقلة (Arena)
**النطاق:** منصة المعلمين الكاملة — Admin → Teachers → Students → Exams → Results على Cloudflare Free.

> هذا التقرير مستقل: لم يُفترض أن تقرير السابق صحيحًا لأن الاختبارات تمر.
> كل بند فُحص بالكود أولًا، ثم بالاختبارات الآلية، ثم بمتصفح Chromium حقيقي (انظر §10).
> كل مشكلة حقيقية وُجدت **أُصلحت** في هذا التسليم، وكل أصل أُعيد تشغيل اختباره.

---

## 1) ملخص التنفيذ (ما الذي كان موجودًا وما الذي فُعِل الآن)

PR #9 كان قد أضاف الهيكل الأساسي: `/teacher` (دخول ثابت بلا تسجيل)، حسابات المعلمين PBKDF2 في KV،
`/api/t/*` (جلسة/لوحة/طلاب/نتائج/كلمة مرور/ملف)، عزل النتائج عبر فهرس `results:teacher:<slug>`،
وقائمة المعلمين في لوحة الإدارة مع ID/teacherCode/slug تلقائي. **البنية المعمارية صحيحة وقُبلت كما هي.**

التدقيق المستقل كشف **8 عيوب حقيقية** — صُححت جميعها في هذا التسليم:

| # | العيب (قبل الإصلاح) | الخطورة | الإصلاح |
|---|---------------------|---------|---------|
| 1 | `GET /api/admin/teachers` (والأرشيف واستجابة الاستعادة) تُعيد سجلات KV الخام **متضمنة `passHash`/`passSalt`** إلى المتصفح؛ وشارة «بيانات الدخول» في اللوحة لا تعمل | عالية (تسريب مواد تجزئة كلمة المرور لأي XSS/إضافات متصفح + خلل UI) | إسقاط آمن عبر `teacherAdminPayload()` (+`isDefault`, `studentLimitStatus`)؛ لم يعد أي endpoint يعيد أي حقل تجزئة |
| 2 | `/api/exam/start` يثق بالـ slug كمدخل حر: **slug وهمي/فارغ** → لا نسب للمعلم → **تجاوز كامل لحد الطلاب** عند التسجيل + نتائج يتيمة خارج كل الفهارس | عالية (تجاوز سعة + تلويث/إفقاد بيانات) | slug غير معروف ⇒ 404؛ فارغ ⇒ يُنسب خادوميًا لمعلم المالك (`ownerTeacher`)؛ لا مسار بعد الآن ينتج نتيجة بلا معلم نشط صالح |
| 3 | إنشاء معلم برابط **معلم مؤرشف** يورّث بصمت فهرس نتائجه القديم | متوسطة (تسريب بيانات بين حسابين) | رفض 409 عند تعارض slug مع الأرشيف — الاستعادة من الأرشيف هي المسار الصحيح |
| 4 | تغيير كلمة مرور المعلم/المسؤول (أو إعادة تعيين المسؤول، أو تعطيل المعلم) **لا يُسقط الجلسات الممنوحة**؛ والتعطيل ثم إعادة التفعيل كانت تُحيي الجلسة القديمة تلقائيًا | متوسطة–عالية (عدم إبطال الجلسات) | **سجلّ جلسات خادومي** `sessv:*` (epochs موقعة داخل الكوكيز، قائمة حتى 8 أجهزة): تسجيل الدخول يضيف، تغيير/إعادة تعيين كلمة المرور أو التعطيل/الأرشفة يمسح — الإبطال فوري عند كل طلب محقق، والجهاز الحالي يعاد إصدار كوكيزه في نفس الرد |
| 5 | تبويب «الملف الشخصي» بلوحة المعلم يعرض الاسم فقط، والحفظ يرسل نبذة/روابط فارغة ⇒ **يمسح بيانات المعلم** | متوسطة (فقدان بيانات) | `GET /api/t/profile` جديد + تعبئة كاملة لكل الحقول + صورة + رابط الطلاب مع زر نسخ (مع حفظ الهاتف) |
| 6 | `teacher.html` المُنشور يحوي هروب جافاسكربت حرفي (`\ud83d\udc68…`) داخل HTML ⇒ نصوص مكسورة ظاهرة في الشريط الجانبي والشعار + فلتة تخطيط | متوسطة (عيوب UI إنتاجية مرئية) | استبدالها بإيموجي حقيقي؛ حارس دائم في ui-qa يمنع أي `\uXXXX` حرفي في HTML |
| 7 | فشل الدخول يُمحى من الشاشة: معالج 401 يعيد رسم نموذج الدخول قبل عرض الرسالة (المعلم والمسؤول) — المستخدم يرى صمتًا | متوسطة (UX أمان) | استثناء endpointَي الـlogin من مسار «انتهت الجلسة»؛ الخطط الآن تعرض الرسالة فعلًا (مُثبت بالمستعرض) |
| 8 | README يعد بإنشاء حساب المسؤول عبر أسرار `ADMIN_INITIAL_EMAIL/PASSWORD` — غير مُنفَّذ؛ وبكشف الحقل الفارغ أولًا لم تكن شاشة «الإعداد الأولي» تظهر مطلقًا في نشر جديد | عالية (توثيق ≠ واقع + طريق تهيئة مكسور) | `ensureAdminBootstrap()` تُنشئ الحساب من الأسرار عند أول طلب إداري (وتُغلق الشاشة العامة تلقائيًا بـ409)؛ فحص الوجود صار قبل فحص الحقول فتظهر شاشة الإعداد فعلًا عند الحاجة |

إصلاحات أمان/متانة إضافية: مقارنة تجزئات بكلمات المرور **زمن-ثابت** (`safeEqualHex`)؛
حفظ الحقول عند PUT الجزئي للمعلم (بريد/هاتف/روابط/العَلَم `enabled` لم تُمسح أو تُفعَّل بصمت)؛
لم تعد صفحة الطالب لمعلم محذوف/معطّل تعرض واجهة عديمة النسب (بطاقة «رابط غير متاح» واضحة)؛
أصول JS/CSS تحمل `?v=` مع HTML مُعاد التحقق (`no-cache` للمسارات الحساسة) لمنع خلط قديم/جديد بعد النشر.

**الواجهة الإدارية** أصبحت تعرض لكل معلم كما هو مطلوب: الاسم، **معرّف المعلم** (ID ثابت + TCH-code)،
الحالة، رابط الطلاب المولّد تلقائيًا، **رابط دخول المعلم الثابت `/teacher`**، البريد،
**حالة بيانات الدخول** (مضبوطة PBKDF2 / بدون كلمة مرور)، حد الطلاب (الحد/الحالي/المتبقي)،
أزرار نسخ لرابط الطلاب ولرابط الدخول.

**عدم المبالغة في البناء (Anti-overbuild):** لم تُضف فوترة، ولا حسابات طلاب، ولا قواعد بيانات منفصلة،
ولا إشعارات/بريد، ولا تحليلات معقدة. اللوحة ظلت: ملف + طلاب + نتائج (+رئيسية مبسطة وإعدادات كلمة مرور).

---

## 2) المعمارية النهائية (3 روابط وظيفية فقط)

```
Browser → Cloudflare Worker (exammanasa-platform)
   /admin          → Admin SPA (لوحة المالك فقط — دخول PBKDF2، لا إنشاء حساب عامًا بعد الإعداد)
   /teacher        → Teacher SPA (دخول ثابت مشترك لكل المعلمين — بلا أي تسجيل ذاتي مطلقًا)
   /<teacher-slug>  → صفحة الطالب لنفس المعلم (index.html بهوية ديناميكية من بيانات المعلم)
   /api/*          → الجلسات/التصحيح الخادومي/لوحة المعلم/إدارة المسؤول
   KV PLATFORM_KV  → المعلمون + الأرشيف + حساب المسؤول + سجل sessv + النتائج + dedupe
   DO AttemptLimiter → عدّادات ذرية: حد المحاولات + سجل الطلاب المتمايزين لكل معلم (سباق-آمن)
   (اختياري) GAS WebApp → جدول Google Sheets نفسه بعمود «المعلم»
```

- **الملكية:** `Admin → Teachers → Students → Exams → Results` — المعلمون يُنشئهم المسؤول فقط.
  الطلاب لا ينشئون حسابات إطلاقًا (اسم + هاتف حسب إعداد المعلم فقط، كما هو مُصمَّم سابقًا).
- **العلامة التجارية:** لا اسم مالك مفروض على المنصة — العناوين المحايدة «منصة الامتحانات»،
  وهوية كل صفحة معلم تأتي من بياناته. اسم النطاق النهائي يُختار خارج هذا التسليم (قابل للاستبدال).
- **الرابط الكامل:** كل المعلمين على نفس النطاق: `platform.pages.dev/ahmed`، `…/teacher`، `…/admin` —
  لا نطاقات فرعية ولا حسابات Cloudflare منفصلة (متوافق مع Free بالكامل).

## 3) الصلاحيات (مفروضة خادميًا)

| الجهة | يسمح | يُمنع (بـ401/403 خادومي لا بالـUI) |
|---|---|---|
| Admin | كل شيء: معلمي CRUD، كلمات مرور المعلمين، تفعيل/تعطيل/أرشفة/استعادة، حد الطلاب، مراجعة/حد المحاولات، أوفلاين، عرض البنك بمفاتيحه، النتائج، CSV، تغيير كلمة مروره | لا يوجد حسابات admin إضافية إلا عبر شاشة الإعداد اليتيمة قبل وجود الحساب (أو أسرار النشر) |
| Teacher | رؤية/تعديل ملفه (اسم/هاتف/نبذة/روابط/صورة)، طلابه فقط، نتائجه فقط، تغيير كلمة مرور جلسته الحية | أي endpoint إداري، أي بيانات معلم آخر (لا معلمرة بـ slug/ID تُقبل من العميل أصلًا)، إنشاء حسابات، تعديل الأسئلة/المفاتيح/الامتحانات، تغيير slug/his email/limits/enabled/role من جانبه |
| Student | الدخول من /<slug>، بدء/تسليم امتحان، نتيجة ومراجعة بعد التسليم | أي مفاتيح قبل التسليم، أي حقن درجة (تُتجاهل)، أي slug وهمي (404) |

**العزل (مُثبت خادميًا):** كل مسارات `/api/t/*` تشتق المعلم من **جلسته الموقعة** (`tid` ثابت غير قابل للتزوير) —
وليس من العميل؛ `teacherId`/`slug` المحقون في body أو query يُتجاهل؛ الفهرس `results:teacher:<slug>` يُقرأ من سجل
الخادم للمعلم المصادَق عليه. المعلم A ← لا نتيجة/طالب/حدود/بيانات دخول للمعلم B (403/401/تجاهل). **معرّف المعلم `t_<hex>` ثابت**:
الاسم قابل للتغيير والـslug لا يتغير إلا صراحةً، والحذف غير تدميري (أرشيف + استعادة) والنتائج وسجل الطلاب لا تُحذف أبدًا،
وسlug المؤرشف لا يُمنح لغيره (منع التوريث الصامت).

## 4) الأمان التنفيذي

- كلمات المرور: **PBKDF2-SHA256، 120k تكرار، ملح عشوائي لكل حساب، مقارنة زمن-ثابت**. لا نص صريح أبدًا؛ لا تسريب تجزئات لأي متصفح.
- الجلسات: توكен `payload.HMAC-SHA256` موقوع بمفتاح الخادم (SESSION_SECRET أو سر مُولَّد في KV)،
  **HttpOnly + Secure + SameSite=Strict + Max-Age** (8 ساعات إداري/معلم، 6 امتحان، 72 وضع أوفلاين)،
  وانتهاء صلاحية + **إبطال خادومي** (§1 بنود 4) عند تغيير كلمة المرور/إعادة التعيين/التعطيل/الأرشفة/الخروج لكل جهاز على حدة.
- CSRF: كوكيز Strict + **إلزام `X-Requested-With: fetch`** لكل كتابة معلم/إداري.
- Rate limiting لكل IP لتسجيل الدخول (المعلم والمسؤول، 10 ثم 15 دقيقة قفل) — best-effort لكل عزل process/ isolate موثق.
- حقن الدرجة/الحقول: الخادم يتجاهل كل شيء في submit سوى `token`+`answers` (عدد تام 0..3 لكل سؤال)؛ التصحيح من `banks.json` الداخلي فقط.
- منع إعادة التسليم: dedupe بمجزوء HMAC للتوكين (KV + Cache API).
- XSS: كل الإدخالات تُمر عبر `esc()`؛ روابط التواصل http(s) فقط (رفض `javascript:` مُختبَر)؛ الصور data-URL png/jpg/webp ≤2.5MB مع تحقق الخادم؛ لا eval/innerHTML بمحتوى مستخدم غير منقٍّ.
- تسريب الأخطاء: استجابات الخطأ رسائل ApiError عربية محددة؛ أي خطأ داخلي → نص عام بلا Stacktrace؛ `X-Content-Type-Options`, `X-Frame-Options: DENY`, Referrer-Policy, Permissions-Policy.
- bank داخل الحزمة فقط؛ `public/` لا يحتوي أي مفاتيح (بوابة E في validate-banks + فحص حي في المتصفح).
- لا أسرار في المستودع: `.dev.vars` محجوب، KV id الافتراضي placeholder، كلمات اختبارات dev-only داخل ملفات الاختبار.

## 5) تدفق الطالب والنسب

`/<slug>` → بيانات الطالب (اسم+هاتف لو طلب المعلم) → المادة → الترم → الموضوع → الدرس → التدريب/الامتحان →
بدء (توكن يحمل slug/seed/nonce خادوميًا) → إجابات كاملة (تسليم ناقص مرفوض) → نتيجة + مراجعة →
**النتيجة منسوبة خادوميًا لصاحب الرابط** (حتى على `/` الجذرية تُنسب للمالك). حد الطلاب: مقعد ذري عند البدء
وإعادة تحقق عند التسليم؛ لا يستطيع عميل صناعة «بلا-نسب» للتهرب.

## 6) سلامة البيانات (مستقلة، لا على عواهنها)

- **Psychology SHA-256:** `8851437be88724537693cbf1ad57c0531e36eca4b55dcea102f7c077b44a8198` — **مطابق حرفيًا** (بوابة D2 في validate-banks؛ catalog/exams/examDefs/questions كلها byte-identical).
- **STRICT_KEYS:** `REVIEW_ANSWER_KEYS.md: 0 open questions` — لا مفاتيح معلقة/مختلقة.
- **بناء-البنك idempotent:** `npm run build:banks` من المصادر `.gs`/JSON يعيد بنوكًا مطابقة حرفيًا للـcommitted (فرق الطابع الزمني فقط، استُعيد) ⇒ لا انحراف بيانات.
- **الحصر الكامل:** 1887 سؤالًا فريدًا (علم نفس 474، فلسفة ت1 823، ت2 590) / 127 امتحانًا (31 + 61 + 35)؛
  الهيكل: مادة ← ترم ← فلسفة/منطق ← موضوع ← درس ← تدريب ← امتحان، مطابقة لعناوين الكتب الرسمية (تقارير AUDIT_PHILOSOPHY*).
- لا تعارض slug داخل أي قائمة نشطة؛ صلاحيات الخيارات 0..3؛ تغطية كل examId في examDefs بلا أيتام (بوابة D).
- لم تُلْمَس ملفات البيانات/المصادر (.gs, data/*.json, *.docx) في هذا التسليم إطلاقًا (git diff يثبت).

## 7) نتائج الاختبارات (على HEAD النهائي)

| البوابة | النتيجة |
|---|---|
| `cloudflare/ npm test` (18 قسمًا / 185 تأكيدًا، ضد wrangler dev بحالة KV نظيفة) | **185 ✓ / 0 ✗** |
| `tools/validate-banks.mjs` (A/B/C/D/E) | **ALL CHECKS PASSED ✓** (تحذير واحد موثق مسبقًا: تكرارات U2-T1 الموروثة المحمية) |
| `STRICT_KEYS=1 tools/review-keys.mjs` | **0 open** |
| `tools/ui-qa.mjs` (jsdom DOM/سلوك على خادم حي — 115 تأكيدًا، منها حرّاس جديدة: بلا `\u` حرفي بالـHTML، لا تسجيل بصفحة المعلم) | **115 ✓ / 0 ✗** |
| بناء الإنتاج: `wrangler deploy --dry-run` | ✅ Worker **4.3MB raw / 359KB gzip** (ضمن كل حدود الخطط، بما فيها سياسة 2026-09-04) + Assets |
| فحص الأسرار بالفرق | ✅ لا شيء |

الاختبارات الجديدة (هذا التسليم) تغطي: scrubbing قوائم الإدارة، حقن الهوية/القيود عبر `/api/t/profile` (يتجاهله الخادم)،
IDOR query-params، لا وجود لمسار تسجيل، إبطال الجلسات (إعادة تعيين/تعطيل/إعادة تفعيل ≠ إحياء/خروج لكل جهاز)،
404 slug وهمي، نسبة الجذر للمالك مع مقعد، حفظ PUT الجزئي، إعادة تعيين المسؤول لكلمة المعلم، قفل المحاولات للمعلم والمسؤول.

## 8) QA متصفح Chromium حقيقي (إلزامي — تم)

**البيئة:** Chromium headless-shell فعلي (بناء 153) عبر playwright-core + `wrangler dev` KV نظيف،
حزم سياقية معزولة (عزل كوكيز حقيقي)، خط Noto Sans Arabic مثبت للمحاكاة الصحيحة.
المجموعة مُوثَّقة كودًا في [`tools/browser-e2e.mjs`](tools/browser-e2e.mjs) — النتيجة: **74 تأكيدًا ✓ / 0 ✗** على HEAD النهائي، وتشمل حرفيًا مصفوفة المطلوب:

- **Admin:** فتح `/admin` → شاشة الإعداد الأولي (بعد إصلاح البند 8) → إنشاء حساب → دخول → المعلمون →
  **+ إضافة معلم** (كل الحقول) → تحقق ID/slug/روابط/بطاقات/نسخ → إضافة بمعرف slug تلقائي → تعديل/تغيير اسم مع الحفاظ على slug →
  تعطيل → إعادة تفعيل → **إعادة تعيين كلمة مرور المعلم من النموذج** → تغيير كلمة مرور المسؤول من واجهة الإعدادات (الجهاز الحالي يبقى عاملاً، القديم يموت).
- **Teacher:** `/teacher` (لا تسجيل) → رفض كلمة خاطئة **مع ظهور الرسالة** → دخول → لوحة (طلاب/نتائج فارغة لعزل B) →
  ملف شخصي pre-fill كامل + حفظ بلا مسح → نسخ رابط → **ممنوع admin (401)** → **ممنوع CSRF (403)**.
- **Student:** من رابط المعلم: بيانات → فلسفة ت1 → موضوع → درس → تدريب → **كل الأسئلة بالنقر الحقيقي** → مراجعة → تسليم → نتيجة، ثم ظهورها في **لوحة المعلم مباشرة** وفي Admin/CSV بعمود المعلم.
- **Security داخل المتصفح:** slug وهمي 404؛ بلا-slug نسبة للمالك؛ حقن درجة/extra-fields يُتجاهل؛ **جسم استجابة `/api/exam/start` المقروء في المتصفح لا يحتوي أي مفتاح** (بما فيه فحص الحقول `["id","no","options","text"]`)؛ جلسة معلم معطّل تموت فورًا؛ صفحة الطالب لمعلم معطّل تعرض البطاقة الواضحة.
- **شهادات لقطات:** `/home/user/qa/shots/` (01 admin setup, 02 teacher cards, 03–05 teacher UI, 06–07 student quiz/result, 09 admin results, 10 disabled, 11–14 mobile) — فُحصت بصريًا.

## 9) موبايل 360×800 / RTL / عربية (متصفح حقيقي بمحاكاة جوال)

✓ لا overflow أفقي إطلاقًا (scrollWidth ≤ 360 على: الرئيسية/الاختيار/الامتحان/النتيجة/دخول المعلم/لوحة المعلم/لوحة المسؤول)،
✓ `dir="rtl"` فعلي، ✓ لا mojibake (لا U+FFFD ولا نصوص مكسورة بعد إصلاح البند 6)، ✓ الخيارات الأربعة كاملة داخل العرض وبأحجام لمس ≥40px،
✓ تدفق الطالب كامل والتسليم والنتيجة على الجوال، ✓ دخول المعلم ولوحته تعملان على الجوال.
(ملاحظة بيئة: sandbox بلا خط إيموجي — تظهر مربعات بدلاً من الإيموجي في اللقطات فقط؛ أي جهاز حقيقي يعرضها.)

## 10) النشر العام الحقيقي — الحالة الصريحة

**تعذر تنفيذ smoke test على نطاق إنتاجي منشور**: لا يوجد نشر سابق معروف لهذا المستودع، وهذه البيئة
**غير موثَّقة لدى Cloudflare** (`wrangler whoami` → not authenticated؛ cdn.playwright.dev/hosts المنصات محجوبة
عndata sandbox). لم تُزعم جاهزية إنتاجية من الاختبارات المحلية وحدها؛ بدلاً من ذلك:
✅ تحققت من حزمة الإنتاج فعليًا عبر `wrangler deploy --dry-run` (4.3MB غير مضغوط / 359KB gzip — ضمن حد Free تاريخيًا 3MB gzip وضمن سياسة 64MiB غير المضغوطة المعلنة في 2026-09-04، ورابط/أصول سليمة)، و✅ شُغّل كل شيء ضد خادم
wrangler dev بنفس workerd runtime بنفس bindings، و✅ وثقت خطوات النشر الحتمية أدناه. **قبل الإعلان "منشور" يجب**:
(1) `npx wrangler kv namespace create PLATFORM_KV` ووضع الـid في wrangler.jsonc، (2) `wrangler secret put`
لـ SESSION_SECRET (+ اختياري ADMIN_INITIAL_* و GAS_WEBAPP_URL/GAS_RESULTS_SECRET)، (3) `wrangler deploy`،
ثم تشغيل نفس مصفوفة §8/§9 على رابط الإنتاج (كوكيز Secure تعمل عبر HTTPS)، والتحقق من DO migrations المطبقة تلقائيًا عند أول deploy.

## 11) جاهزية Cloudflare Free (مراجعة)

- Worker واحد + Static Assets من نفس الحزمة (لا حساب/مشروع لكل معلم) — ✅.
- الحدود: طلبات 100k/يوم، CPU 10ms/طلب — كل مسار يقرأ KV مرة–مرتين كحد أقصى؛ الجلسة/العزل لا يضيفان إلا قراءة KV خفيفة واحدة مسجلة (sessv) على مسارات المعلم/الإدارة؛ `results` مقصورات (100 recent/200 لكل معلم) بلا مسح فهارس ضخم؛ لا subrequests خارجية في المسار الساخن.
- KV: المعلمون سجل واحد صغير؛ dedupe TTL 7d؛ الجلسات لا تُكتب (stateless + قوائم sessv صغيرة).
- Assets caching: `max-age=3600` لـJS/CSS مع `?v=`، فهرسة HTML بـ304، `Cache-Control: no-cache` لأغلفة الجلسات — مناسب.
- Durable Objects (AttemptLimiter) ضمن Free؛ لا D1 (غير مطلوب؛ لا مبالغة)؛ observability معطلة (لا logs حساسة).
- `robots.txt` + `noindex` للوحات — ✅. حزم ≤ حد Free ✅ (359KB gzip).

## 12) حدود معروفة غير حاسمة (Non-blockers موثقة)

1. Rate limiting وlogin-fails **لكل isolate** (best-effort تحت Free بدون DO للعدّادات) — قفل IP مؤقت، لا ضمان عالمي.
2. فهارس النتائج مقصوصة (آخر 100 إداري / 200 لكل معلم) مع **النسخة الدائمة في Google Sheets** (كل النتائج مُمرَّرة عند ضبط GAS)؛ سجل الطلاب المتمايز **دقيق وغير مقصوص** (DO).
3. الإبطال يُسقط الجلسات فورًا؛ التوكن نفسه يبقى مقبول التوقيع حتى Max-Age لطلبات لم تُسجَّل بعد (نموذج stateless معتمد).
4. شاشة الإعداد الأولي مفتوحة نظريًا في نافذة ما قبل أول حساب **إذا لم تُستخدم أسرار ADMIN_INITIAL_*** — يُنصح بنشر السرر مباشرة بعد أول deploy (الشاشة تُغلق ذاتيًا بـ409 بعدها).
5. تغيير slug المعلم يدويًا من الإدارة ينقل لوحة النتائج (الفهرس keyed بالslug) — لا تحويلات؛ موثق للإدارة، لا يحدث إلا بطلب صريح.
6. تكرارات U2-T1 الموروثة من المصدر المحمي (Data.gs) محفوظة عمدًا كما هي — موثقة في AUDIT_EDUCATIONAL_V2.md.

## 13) متطلبات التسليم (Deploy runbook)

```bash
cd cloudflare && npm install
npx wrangler login
npx wrangler kv namespace create PLATFORM_KV      # ضع الـid في wrangler.jsonc
npx wrangler secret put SESSION_SECRET            # توقيع الجلسات
npx wrangler secret put ADMIN_INITIAL_EMAIL       # (موصى به — يغلق شاشة الإعداد للأبد)
npx wrangler secret put ADMIN_INITIAL_PASSWORD
# اختياري لجدول Google Sheets الحالي:
npx wrangler secret put GAS_WEBAPP_URL
npx wrangler secret put GAS_RESULTS_SECRET
npx wrangler deploy
# اختياري: عند تغيير بيانات البنوك: npm run build:banks && node ../tools/validate-banks.mjs
```

## 14) قائمة القبول النهائية

[✓] origin/main (ef50b13) فُحص؛ PR#9 روجع مستقلًا كامل الـdiff · [✓] حسابات المعلمين وإنشاء المسؤول-فقط يعملان
[✓] `/teacher` ثابت بلا تسجيل · [✓] PBKDF2/unique-salt/120k/مقارنة آمنة · [✓] Teacher ID ثابت وslug فريد/مستقر مع حماية الأرشفة
[✓] رابط الطلاب يولَّد تلقائيًا (من الاسم) · [✓] النتائج تُنسب خادوميًا (بما فيها الحالتان 404/الجذر) · [✓] عزل المعلمين مُثبت خادوميًا وفي المتصفح
[✓] المعلم لا يصل للإدارة ولا يحرر المحتوى المركزي · [✓] المفاتيح مخفية (متصفح حقيقي + بوابة E) · [✓] Psychology SHA مطابق
[✓] STRICT_KEYS نظيف · [✓] validate كامل · [✓] 185/185 + 115/115 + build idempotent · [✓] Chromium E2E 74/74
[✓] موبايل 360×800 بلا overflow · [✓] RTL بلا mojibake (وحارس دائم ضده) · [✓] لا أسرار الإنتاج بالـrepo · [✓] Free-ready (dry-run 359KB)
[✓] وثيقة التسليم هذه · [~] نطاق إنتاج حي: متعذر من بيئة معزولة بلا اعتماد Cloudflare — الـrunbook في §13 (صراحةً، لا ادعاء زائف)
