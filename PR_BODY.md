# PR body — teacher platform FINAL (paste with: gh pr create … --body-file PR_BODY.md)

## التدقيق المستقل النهائي لمنصة المعلمين — 8 عيوب حقيقية وُجدت وصُححت

### ما يصلحه هذا PR فوق PR #9 (ef50b13)
1. **تسريب مواد كلمة المرور**: `GET /api/admin/teachers` (والأرشيف واستجابة الاستعادة) كانت تعيد سجلات KV الخام (`passHash`/`passSalt`/`passIterations`) للمتصفح → أُبدلت بإسقاط آمن `teacherAdminPayload`؛ وشارة حالة بيانات الدخول صارت تعمل.
2. **تجاوز حد الطلاب ونتائج يتيمة**: `POST /api/exam/start` كان يقبل slug حرًا: وهمي ⇒ نتيجة خارج كل فهرس بلا مقعد مُسجَّل؛ فارغ ⇒ بلا نسب. الآن: غير معروف ⇒ 404؛ فارغ (صفحة الجذر) ⇒ يُنسب خادوميًا للمالك مع تسجيل مقعد ذري.
3. **توريث نتائج المؤرشفين**: إنشاء معلم جديد برابط يخص معلمًا مؤرشفًا كان يرث فهرس نتائجه بصمت ⇒ 409 (المسار الصحيح: الاستعادة من الأرشيف).
4. **إبطال خادومي للجلسات**: تعطيل/أرشفة/إعادة تعيين/تغيير كلمة مرور لم تقتل الجلسات الممنوحة (وإعادة التفعيل كانت تُحيي الجلسة القديمة). أُضيف سجل epochs في KV (`sessv:*`، حتى 8 أجهزة) مع إعادة إصدار كوكيز الجهاز الحالي عند التغيير الذاتي.
5. **مسح صامت عند حفظ ملف المعلم**: تبويب الملف يعرض الاسم فقط ويرسل بقية الحقول فارغة ⇒ `GET /api/t/profile` جديد + تعبئة كاملة + صورة + رابط طلاب مع زر نسخ.
6. **teacher.html يعرض محارف هروب JS حرفيًا** (إيموجي الشريط الجانبي/الشعار `\ud83d\udc68` كنص مكشوف) ⇒ استُبدلت برموز فعلية + حارس دائم في ui-qa.
7. **رسالة فشل الدخول تُمحى**: معالج 401 يعيد رسم نموذج الدخول قبل عرض الخطأ (معلم/مسؤول) ⇒ استثناء endpoint الدخول؛ الخطأ يظهر الآن فعلًا.
8. **README ≠ واقع**: أسرار `ADMIN_INITIAL_EMAIL/PASSWORD` كانت موثقة وغير منفذة، وشاشة «الإعداد الأولي» لا تظهر مطلقًا في نشر جديد (فحص الحقول قبل فحص الوجود) ⇒ `ensureAdminBootstrap` + تقديم فحص الوجود.

إضافات أمان/متانة: مقارنة تجزئات زمن-ثابت؛ PUT جزئي يحافظ على email/phone/enabled (بلا مسح أو تفعيل صامت)؛ بطاقة واضحة للطالب على رابط معلم محذوف/معطّل؛ `?v=` للأصول مع no-cache لأغلفة اللوحات؛ الاختبارات تغطي قفل المحاولات للمعلم والمسؤول.

### الأدلة (على HEAD النهائي)
- `npm test`: **185 ✓ / 0 ✗** (18 قسمًا؛ +29 تأكيدًا جديدًا).
- `tools/ui-qa.mjs`: **115 ✓ / 0 ✗** · `tools/validate-banks.mjs`: **PASS** · **Psychology SHA-256 `8851437be88724537693cbf1ad57c0531e36eca4b55dcea102f7c077b44a8198` مطابق** · STRICT_KEYS: **0 open** · `build-banks` idempotent (فرق الطابع الزمني فقط، استُعيد banks.json كما هو).
- **Chromium حقيقي** (`tools/browser-e2e.mjs`): **74 ✓ / 0 ✗** — مسؤول (إعداد أولي→إضافة معلم→بطاقات كاملة→تعطيل/إعادة تفعيل→إعادة تعيين كلمة مرور المعلم→تغيير كلمة مرور المسؤول)، معلم (`/teacher` بلا تسجيل، لوحة، ملف، **عزل تام** 401/403)، طالب (تدفق كامل من رابط المعلم → نتيجة → ظهورها في لوحة المعلم + CSV بعمود المعلم)، **جسم استجابة `/api/exam/start` في المتصفح بلا مفاتيح**، حقن درجة مُتجاهَل، CSRF مفروض.
- **موبايل 360×800 RTL**: لا overflow في كل الشاشات، لمس ≥40px، لا mojibake، لقطات موثقة.
- `wrangler deploy --dry-run`: 4.3MB raw / 359KB gzip ✓ — ولا أسرار إنتاج في المستودع.

### غير منفَّذ (بصراحة)
نشر إنتاجي حي + smoke test على نطاق عام: غير متاح في بيئة العمل (لا اعتماد Cloudflare) — runbook التثبيت الكامل في `FINAL_TEACHER_PLATFORM_AUDIT.md` §10/§13.
