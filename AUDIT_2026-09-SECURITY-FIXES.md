# تقرير التدقيق والإصلاح — منصة الامتحانات (Cloudflare)

**التاريخ:** 2026-09-13 · **المستودع:** `drmostafatito-alt/exammanasa`

| البند | الحالة |
|---|---|
| الفرع | `arena/01a0984b-exammanasa` |
| بداية العمل | `47c62df` (وهو نفسه آخر `origin/main` — لم يتحرّك `main` أثناء العمل) |
| نهاية العمل | `930022c` |
| الالتزامات | 14 التزامًا |
| الدفع | `git push origin arena/01a0984b-exammanasa` ✓ — البعيد على نفس SHA (`930022c`) |
| الشجرة (working tree) | **نظيفة** — `git status --porcelain` بلا مخرجات |

> **ملاحظة على الفرع:** طلبتَ العمل على `main` مباشرةً. هذه الجلسة مثبَّتة على الفرع
> `arena/01a0984b-exammanasa` (بنية تحتية لـ Arena لا يمكن تغييرها من طرفي)، ولذلك
> نُفِّذ كل العمل ودُفع عليه. محتواه جاهز للدمج في `main` بدمج سريع (fast-forward)
> دون أي تعارض، لأنه متفرّع من قمة `main` نفسها.

---

## ١) المشكلات المكتشفة (المشكلة · الخطورة · السبب · الإصلاح)

### أ. أمان (P0)

| # | المشكلة | الخطورة | السبب الجذري | الإصلاح |
|---|---|---|---|---|
| 1 | الترويسات الأمنية كانت تُغفَل عن معظم الاستجابات | عالية | السلوك الافتراضي لـ Cloudflare يخدم أي ملف موجود في `public/` **قبل** تشغيل الـWorker، فتُسلَّم `/` و`/admin` و`/teacher` وكل `.js`/`.css` بلا CSP ولا `nosniff` ولا `X-Frame-Options` | `assets.run_worker_first = true` في `wrangler.jsonc`، فيمرّ كل طلب على `securityHeaders()`. أُضيف CSP، وتحويل 308 لـ `/index.html` و`/admin.html` و`/teacher.html` إلى مساراتها القياسية، ودعم `HEAD` على `/api/*` |
| 2 | `/api/admin/setup` (إنشاء حساب المالك) بلا حماية CSRF | **حرجة** | الفحص كان يغطي المسارات التي تتطلب جلسة فقط، فكان بإمكان أي موقع خارجي إنشاء حساب مسؤول أول على نشر جديد | `/api/admin/setup` و`/api/t/login` يخضعان الآن لنفس المنظومة الثلاثية: ترويسة `X-Requested-With` + تطابق `Origin` مع `Host` + `SameSite=Strict` |
| 3 | فحص `Origin` كان يقبل أي مصدر عند غياب `Host` | متوسطة | لم تكن هناك معالجة لحالات الـtunnel/الـproxy | `hostOf` / `isLoopbackHost` / `crossSiteRequest` تُطبَّع المضيف وتسمح باللوكل فقط |
| 4 | حقن الصيغ في تصدير CSV | متوسطة | قيمة تبدأ بـ `=` `+` `-` `@` تُقرأ كصيغة عند فتح الملف في Excel/Sheets | `esc()` يسبق القيمة بفاصلة عليا |
| 5 | محارف تحكّم (control chars) في المخرجات النصية | منخفضة | تُفسد CSV وقد تُستخدم في log-injection | `stripControl()` على المخرجات النصية |
| 6 | مقارنة توقيع جلسة الامتحان غير ثابتة الزمن | منخفضة | `===` على السلاسل | `safeEqualHex()` |
| 7 | خريطة حدّ المحاولات تنمو بلا حد | منخفضة | الخريطة تعيش بعمر الـisolate ولا تُنظَّف إلا عند دخول ناجح | `sweepThrottles()` تكنس المنتهي وتفرض سقفًا صلبًا |

### ب. سلامة البيانات (P0/P1)

| # | المشكلة | الخطورة | السبب الجذري | الإصلاح |
|---|---|---|---|---|
| 8 | **تسليمات متزامنة تُفقد من فهرس النتائج** | **حرجة** | `results:recent` و`results:teacher:<slug>` مفتاحا KV مفردان يُحدَّثان بـ read-modify-write؛ ست تسليمات متزامنة أسقطت أربعة منها من الفهرس (جسّمناها: ٢ من ٦ فقط ظهرت للوحة المعلم) | الإلحاق أصبح داخل معاملة تخزين في كائن `AttemptLimiter` (متسلسل وذري)، وKV صار مرآة write-through. القراءة تدمج الاثنين وتُلغي التكرار بالـ`id`، فتبقى النتائج المسجَّلة قديمًا ظاهرة. احتياطي للوضع السابق إن غاب ربط الـDO |
| 9 | **صور المعلمين داخل مستند `teachers` المفرد** | **حرجة** | حدّ القيمة الواحدة في KV هو 25 MiB، والصورة قد تبلغ 2.5 MiB من base64 → نحو ١٠ معلمين بصور كان يكفي لإيقاف كل كتابة على القائمة (لا إضافة ولا تعديل ولا إعادة تعيين كلمة مرور) | `saveTeachers(env, teachers, photoUpdates)` يجرّد الصور، ويحفظ كل صورة في `teacher:photo:<id>` عند تغيّرها فقط، مع حاجز 20 MiB يرفض برسالة 413 واضحة بدل 500 غامض. مسار الامتحان الساخن لا يقرأ الصور أبدًا |
| 10 | تغيير `slug` معلم كان يُيتّم نتائجه ويصفّر حدّ طلابه | عالية | الفهارس مفتاحها الـslug، وعداد الطلاب مفتاحه الـslug | `slug` ثابت لا يتغيّر بتعديل الاسم؛ والفهارس والعدادات مفتاحها `teacher:<id>` الثابت |
| 11 | `DEFAULT_TEACHERS` كان يُعادة مرجعه مباشرةً | متوسطة | أي تعديل عابر كان يتسرّب إلى ثابت الوحدة (module-level) | `getTeachers` يعيد نسخًا |
| 12 | تعارض أكواد المعلمين | منخفضة | توليد غير مضمون التفرد | `nextTeacherCode` يضمن التفرد |

### ج. التوافر وتجربة الاستخدام (P1/P2)

| # | المشكلة | الخطورة | السبب الجذري | الإصلاح |
|---|---|---|---|---|
| 13 | **قفل محاولات الدخول يخفي شاشة الإعداد الأولي عن المالك** | عالية | واجهة الإدارة كانت تكتشف «أول تشغيل» بإرسال بيانات دخول فارغة إلى `/api/admin/login` — وهو مسار خاضع لحدّ المحاولات، فاستنفاد المحاولات من نفس IP كان يخفي شاشة الإعداد نهائيًا على نشر جديد | `GET /api/admin/status` (غير خاضع للحدّ)؛ الواجهة تستخدمه أولًا، والمسار القديم يبقى احتياطًا للتوافق مع نشر قديم |
| 14 | 403 في واجهة الإدارة يترك اللوحة في حالة مكسورة | متوسطة | `api()` كان يعالج 401 فقط | يعالج 401 و403 ويعرض شاشة الدخول برسالة الخادم العربية، على غرار `teacher.js`. `renderLogin(opts)` يقبل رسالة خطأ دون فتح شاشة الإعداد بالخطأ |
| 15 | طلبات مكرّرة عند بدء لوحة الإدارة | منخفضة | `/status` ثم `/session` ثم محاولة دخول فارغة | `/api/admin/status` يعيد أيضًا `authed` والبريد (إن كانت الجلسة سارية) → **طلب واحد** يبدأ اللوحة. التسلسل الآن: `status → login → session → overview` |

### د. أجنحة الاختبار نفسها

| # | المشكلة | الإصلاح |
|---|---|---|
| 16 | `e2e-cms.mjs` يفترض وجود حساب مسؤول مسبقًا؛ على قاعدة جديدة كانت الشاشة الأولى «إنشاء الحساب» فيفشل الانتظار | يفحص `/api/admin/status` وينفّذ الإعداد أولًا عند الحاجة؛ وأُصلح مسار ملف الاستيراد المكتوب في `/tmp/shots` غير المُنشأ |
| 17 | `browser-e2e.mjs` يفحص **نص** رسالة الخطأ لمعلم معطّل، فكان يفشل متقطّعًا حسب ترتيب التشغيل | يُفحص النتيجة: لا جلسة + رسالة خطأ ظاهرة |
| 18 | فحوصات الجوال كانت في سكربتات مؤقتة تُمحى | صارت مجموعتين دائمتين `[V4]`/`[V5]` في `tools/visual-qa.mjs` |

---

## ٢) الملفات المعدَّلة

```
cloudflare/README.md         |  16 +-   حدود الخطة + سبب run_worker_first + حجم قيمة KV
cloudflare/public/admin.js   |  41 +-   /api/admin/status، معالجة 403، renderLogin(opts)
cloudflare/public/app.js     |   3 +-
cloudflare/src/worker.js     | 400 +++-- معظم الإصلاحات أعلاه
cloudflare/tests/run-all.mjs | 233 +++-- 6 مجموعات جديدة (١ب، ١٩ب، ١٩ج، ١٩د، ١٩ه، ١٩و)
cloudflare/wrangler.jsonc    |   8 +-   run_worker_first
tools/browser-e2e.mjs        |  16 +-
tools/e2e-cms.mjs            |  21 +-
tools/visual-qa.mjs          | 100 +++   مجموعتا الجوال الدائمتان
---------------------------------------------------------------
9 files changed, 763 insertions(+), 75 deletions(-)
```

---

## ٣) الاختبارات المنفَّذة (كلها على الخادم الحقيقي، لا محاكاة)

| الجناح | الأمر | النتيجة |
|---|---|---|
| الواجهة البرمجية / التكامل / الأمان | `cd cloudflare && npm test` | **294 ✓ / 0 ✗** (كان 266 قبل العمل) |
| بنوك الأسئلة | `npm run validate` | **ALL CHECKS PASSED** ✓ |
| متصفح حقيقي (طالب/معلم/مسؤول) | `tools/browser-e2e.mjs` | **129 ✓ / 0 ✗** |
| فحص الواجهة (jsdom) | `tools/ui-qa.mjs` | **128 ✓ / 0 ✗** |
| البصري/الهندسي | `tools/visual-qa.mjs` | **80 ✓ / 0 ✗** (كان 29) |
| CMS (تحرير/استيراد/تصدير/سلة) | `tools/e2e-cms.mjs` | **21 ✓ / 0 ✗** |
| **الإجمالي** | | **652 تأكيدًا، كلها ناجحة** |

**مجموعات الاختبار الجديدة (دليل على أن كل إصلاح محمي من الارتداد):**
`[1ب]` ترويسات الأمان والتحويلات و`HEAD` · `[19ب]` ثبات الـslug وتميّز الأكواد ·
`[19ج]` CSRF + تعقيم CSV/المحارف · `[19د]` صورة المعلم في مفتاح مستقل ·
`[19ه]` رحلة تصدير ←→ استيراد · `[19و]` ذرية فهرس النتائج تحت التزامن.

**إثبات أن الاختبار يكشف العلّة:** عُطِّل `resultIndexAppend` مؤقتًا ففشلت مجموعة
`[19و]` بـ٤ من ٨ تأكيدات (فُقدت ٤ نتائج من ٦) — ثم أُعيد الكود وعادت خضراء.

---

## ٤) مصفوفة الأمان — نتيجة الفحص

| الفحص | النتيجة |
|---|---|
| طالب → واجهات المسؤول/المعلم | **مرفوض** ✓ |
| معلم أ → بيانات معلم ب | **مرفوض** ✓ |
| معلم → واجهات المسؤول | **مرفوض** ✓ |
| جلسة معلم معطّل/مؤرشف | **مرفوضة** ✓ |
| كلمة المرور القديمة بعد إعادة التعيين | **مرفوضة** ✓ |
| slug غير موجود | **404** ✓ |
| إعادة استخدام slug مؤرشف | **آمنة** ✓ |
| تزوير معرّف المعلم / ملكية النتيجة / ملكية الامتحان | **مرفوض** ✓ |
| CSRF مفقود أو خاطئ (بما فيه `/api/admin/setup` و`/api/t/login`) | **مرفوض** ✓ |
| `correctAnswer` في حمولة الطالب | **غائب** ✓ (مؤكَّد آليًا في كل مسار) |
| `passHash` / `passSalt` / `passIterations` في حمولة المعلم | **غائبة** ✓ |
| حدّ الطلاب (ذرّي، بلا مقاعد مكرّرة/يتيمة) | **مؤكَّد تحت التزامن** ✓ |
| منع إعادة التسليم | **مؤكَّد** ✓ |

---

## ٥) فحوصات الجوال (360×800، 390×844، 430×932) — قياس فعلي في Chromium

| الفحص | 360 | 390 | 430 |
|---|---|---|---|
| لا overflow أفقي | ✓ | ✓ | ✓ |
| RTL + `exam-mode` | ✓ | ✓ | ✓ |
| ٤ خيارات، كل منها ≥44px ارتفاعًا | ✓ | ✓ | ✓ |
| نص السؤال أعلى الخيارات (بلا بحث) | ✓ | ✓ | ✓ |
| زر «التالي» داخل الشاشة (بلا تمرير) و≥48px | ✓ | ✓ | ✓ |
| زر «التالي» لا يغطي أي خيار | ✓ | ✓ | ✓ |
| السؤال التالي يبدأ من أوله (`scrollTop=0`) | ✓ | ✓ | ✓ |
| سؤال طويل جدًا (220 كلمة): المنطقة تمرّر داخليًا | ✓ | ✓ | ✓ |
| سؤال طويل جدًا: «التالي» يبقى ظاهرًا ولا يعلو الخيارات | ✓ | ✓ | ✓ |
| الشريط السفلي 56–68px، ٤ عناصر، أيقونات ≤24px | ✓ (57px) | ✓ (57px) | ✓ (57px) |
| المحتوى والتذييل لا يُغطَّيان بالشريط | ✓ | ✓ | ✓ |
| لا أخطاء كونسول أثناء الامتحان | ✓ | ✓ | ✓ |

---

## ٦) سلامة البيانات

| البند | القيمة |
|---|---|
| SHA-256 لعلم النفس (catalog + exams + examDefs + questions) | `8851437be88724537693cbf1ad57c0531e36eca4b55dcea102f7c077b44a8198` — **مطابق، لم يتغيّر** ✓ |
| عدد الأسئلة | 1887 (نفس 474 / فلسفة ت1 823 / فلسفة ت2 590) |
| عدد الامتحانات | 127 (نفس 31 / 61 / 35) |
| هل تغيّرت بيانات نفسية/أسئلة/خيارات/مفاتيح؟ | **لا** — صفر تغيير على المحتوى |
| هل حُذف شيء من البيانات الحقيقية؟ | **لا**. التعديل الوحيد على بنية التخزين هو نقل صور المعلمين إلى مفاتيح `teacher:photo:<id>` (نفس المحتوى)، وإضافة فهرس النتائج في الـDO (لا يحلّ محلّ `result:<id>`؛ الاثنان يُدمجان) |
| التكرارات القديمة المعروفة `U2-T1 ×6` و `U2-COMPREHENSIVE ×3` | **متروكة كما هي وموثَّقة** ✓ |

---

## ٧) تحذيرات حقيقية متبقية (بلا مبالغة)

1. **تحذير واحد في `npm run validate`** — تكرارات `U2-T1 ×6` و`U2-COMPREHENSIVE ×3` القديمة، موثَّقة ومطلوب عدم حذفها آليًا. لا يؤثر على الطالب (الامتحانات تُبنى من تعريفات صريحة).
2. **`run_worker_first` يكلّف طلبات** — كل طلب (بما فيه الأصول الثابتة) يمرّ الآن على الـWorker ويُحسب من حدّ ١٠٠ ألف/يوم في الخطة المجانية. خُفِّف بـ`Cache-Control: max-age=3600` لملفات JS/CSS/SVG، وموثَّق في `README` مع طريقة الرجوع (`_headers`) إن تجاوزت الحدّ.
3. **`ensureAdminBootstrap`** — نافذة سباق نظريّة جدًا بين طلبين متزامنين على نشر جديد بمفاتيح `ADMIN_INITIAL_*`؛ الأسوأ إنشاء الحساب مرتين بنفس البيانات، ولا أثر أمني (المسار مغلق بـ409 بعد أول إنشاء).
4. **الـ404 لـslug غير موجود** يُخدم مع جسم الصفحة الرئيسية (حالة 404 صحيحة، والمحركات لا تفهرس 404). مقبول، ويمكن تحسينه لاحقًا بصفحة 404 مستقلة.
5. **سقف فهرس النتائج** 400 نتيجة لكل معلم و400 عامة (النتائج القديمة تبقى محفوظة في `result:<id>` وتظهر من مرآة KV ما دامت فيها). رفع السقف يستلزم ترحيلًا إلى分页 حقيقي.

---

## ٨) قرار بعدم التنفيذ (وسببه)

- **نقل كل فهرس النتائج إلى Durable Object بالكامل**: أُلغي لأنه يستلزم ترحيلًا معقّدًا للبيانات القائمة ومخاطرة ارتداد أعلى من العائد؛ الحل المُنفَّذ يحقّق الذرية المطلوبة مع إبقاء KV مرآة ونسخة احتياطية.
- **إعادة تصميم الواجهة**: ممنوع صريحًا في الطلب، وكل التعديلات بقيت ضمن الهوية البصرية القائمة (RTL، تدرّجات أزرق/كريمي/ذهبي، أشكال عضوية).
