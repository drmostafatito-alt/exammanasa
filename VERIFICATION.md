# VERIFICATION.md — قائمة التحقق النهائية للقبول (سبتمبر 2026)

> وثيقة مطابقة لقائمة القبول المطلوبة بندًا بندًا، مع الدليل لكل بند.
> **ما لم يُذكر خلاف ذلك، كل بند أدناه مُتحقق آليًا (اختبارات خادم 69/69 + فحص واجهة 60/60 + تحقق بيانات كامل) وقابل لإعادة الإنتاج من المستودع.**

## أولًا: حدود المصادر — بصدق كامل (§17-18)

قبل قائمة القبول، هذه هي الحالة الفعلية للمصادر — **لم يُراجَع أي مصدر لم يُقرأ فعلًا، ولم تُملأ الفجوات بمعرفة عامة**:

| المصدر | الحالة الفعلية | الأثر |
|---|---|---|
| كتاب الشرح — فلسفة ومنطق ت1 (PDF) | ✅ قُرئ (حتى حدود محلّل PDF ~30 صفحة) | مصدر بنية ت1 الرسمية (الوحدات/الفصول/الدروس) |
| كتاب أسئلة فلسفة ت1 (docx) | ✅ قُرئ كاملًا (48 مقطعًا) | المصدر الحرفي لبنك ت1 (619 سؤالًا) |
| كتاب أسئلة فلسفة ت2 (docx) | ✅ قُرئ كاملًا (24/24 مقطعًا) | المصدر الحرفي لبنكي ت2: الفلسفة (254) + المنطق (202) |
| كتاب أسئلة علم النفس 1 و2 (docx) | ✅ قُريا (مقاطع موزعة + مطابقة صف-بصف آلية للبنك المحمي) | مصدر بنك علم النفس (474) + توثيق 8 تصحيحات مفاتيح |
| كتاب الشرح — علم النفس (PDF ‏65.5MB) | ❌ **تعذر الفتح** (HTTP 500 مرتين) | لم يُستخدم إطلاقًا؛ بنك علم النفس تحقق من كتاب الأسئلة (المصدر الثانوي الرسمي) لا من الشرح |
| كتاب الشرح — فلسفة/منطق ت2 | ❌ **غير موجود في مجلد المصادر** | انظر «التحقق التعليمي» أدناه — كيف عولجت منطق ت2 دون تخمين |

**التحقق التعليمي لمنطق ت2 بالتفصيل (بدون كتاب الشرح):** المصدر الثانوي الرسمي (كتاب الأسئلة) قُرئ كاملًا؛ عناوين الفصول والتدريبات مأخوذة حرفيًا من عناوينه؛ مفاتيح الإجابة تحققت يدويًا سؤالًا بسؤال من النموذج المعرفي الكامن في نصوص التدريبات نفسها (تعريفات/أمثلة/مقولات متكررة) + التقاطع مع بنك ت1. **كل سؤال تعذر تحديد مفتاحه قاطعًا استُبعد (6 أسئلة موثقة الأسباب — أغلبها الإجابة الصحيحة غير موجودة أصلًا بين خيارات السؤال)** — لا يوجد أي مفتاح مخمَّن في المنصة. 4 حالات تحفظ موثقة للمراجعة عند توفر كتاب الشرح (AUDIT_PHILOSOPHY_TERM2_LOGIC.md §5). **هذه هي الحالات التي يتطلب حسمها النهائي كتاب شرح ت2 — وهو التبعية الوحيدة المفتوحة.**

**مراجعة توزيع الامتحانات أكاديميًا (§14):** 83 امتحانًا ليست هدفًا رقميًا بل نتيجة التغطية الصحيحة: كل موضوع دراسي في المنهج له امتحان واحد على الأقل (تدريبات ت1 الفلسفة حتى 3 نماذج غير متراكبة، منطق ت2 نموذج واحد لحجم بنك كل تدريب 20-36 سؤالًا — أي نموذج ثانٍ كان سيتطلب تكرار أسئلة)، + 9 امتحانات شاملة (6 وحدات علم النفس + شامل منهج علم النفس 60 سؤالًا + شامل وحدة منطق ت2 + شامل ت1 + شامل ت2 بـ40 سؤالًا). لا يوجد أي سؤال مكرر داخل امتحانات المنطق الجديدة، ولا امتحان «محشو» لزيادة العدد.

**الامتحانات الشاملة لعلم النفس (§16):** موجودة ومراجعة — 6 شاملة وحدات (موروثة من المصدر) + **PSY-FULL-COMP (60 سؤالًا: 10 لكل وحدة)** مبني بأسئلة موثقة قائمة فقط بتوزيع متناوب موثق — لا يكرر أي امتحان قائم.

## ثانيًا: قائمة القبول بندًا بندًا (§28)

### التصميم والواجهة

| البند | الحالة | الدليل |
|---|---|---|
| وضع فاتح فقط | ✅ | styles.css مُعاد كتابته كاملًا: خلفية فاتحة `#F4F7F9`، بطاقات بيضاء، أخضر زمردي + ذهبي. **لا وضع داكن، لا مبدّل، لا `prefers-color-scheme: dark`** (مفحوص آليًا) |
| رئيسية احترافية | ✅ | قسم معلم Hero كامل: بطاقة متدرجة ناعمة، ظلال خفيفة، حواف دائرية 26px |
| مساحة صورة كبيرة للمعلم | ✅ | صورة 218px بإطار أبيض وشارة ذهبية (تُصغّر أنيقًا على الموبايل)؛ عند غياب صورة حقيقية تظهر حالة فارغة أنيقة (Monogram) — **لا صورة وهمية** |
| هوية المعلم الصحيحة | ✅ | الاسم الحقيقي «د. مصطفى تيتو» + التخصص + العام الدراسي من بيانات فعلية (Code.gs الأصلي + فهرس المنصة). زر واتساب **يظهر فقط إذا أُعدّ رابطه** (حاليًا غير مُعد — يُضاف من لوحة التحكم) |
| بطاقات مواد احترافية | ✅ | بطاقتان (علم النفس/الفلسفة والمنطق) بأيقونات ملونة، وصف حقيقي، وأرقام **محسوبة من البيانات** (31/52 امتحانًا — مفحوص آليًا)، وزر ابدأ |
| بطاقات امتحانات احترافية | ✅ | بطاقة exam-card: رقم الموضوع + الاسم الحرفي + عدد الأسئلة + مستوى الصعوبة (من بيانات حقيقية) + سهم ابدأ؛ الشاملة بلون ذهبي مميز |
| أرقام الموضوعات | ✅ | كل بطاقة تعرض «موضوع N» فوق الرقم — مفحوص آليًا لكل المواد |
| أسماء الدروس الحرفية | ✅ | أسماء الدروس تأتي حرفيًا من الفهرس (مفحوص آليًا: أول درس بعنوانه الكامل) |
| التنقل الأكاديمي الصحيح | ✅ | علم النفس: مادة←وحدة←موضوع؛ الفلسفة: مادة←**ترم**←وحدة←فصل←موضوع — بالتسمية الرسمية (الوحدة الأولى: الفلسفة / الوحدة الثانية: المنطق) مفحوصة آليًا |
| تجربة امتحان سلسة | ✅ | «السؤال 7 من 20»، شريط تقدم، شريط أرقام بمؤشرات مُجاب/غير مُجاب، السابق/التالي، حفظ تلقائي للإجابات أثناء التنقل (sessionStorage)، انتقالات ناعمة، بدون أي إعادة تحميل (SPA) |
| منع التسليم الناقص | ✅ | **مزدوج**: الواجهة (زر معطّل + تحديد الأسئلة الناقصة رقمًا + الانتقال إليها) والخادم (رفض 400 مع قائمة الأسئلة غير المُجابة `unanswered` — مفحوص آليًا بالاختبار 6b) |
| الدرجة الفورية | ✅ | تصحيح على الخادم فور التسليم: الدرجة، النسبة المئوية، الصحيحة، الخاطئة |
| الإجابات الصحيحة بعد التسليم فقط | ✅ | المفاتيح لا تصل للمتصفح قبل التسليم إطلاقًا (فحص تسريب آلي [E] + اختبار دورة درجة كاملة لكل الامتحانات الـ83) |
| إظهار الأخطاء بوضوح | ✅ | لكل سؤال: ✅/❌ + «إجابتك» + «الإجابة الصحيحة» للخاطئ — مفحوص آليًا |
| لوحة تحكم بسيطة | ✅ | 8 أقسام فقط: نظرة عامة/المعلمون/علم النفس/الفلسفة والمنطق/بنك الأسئلة/الامتحانات/النتائج/**الإعدادات** (تغيير كلمة المرور + معلومات المنصة) |

### المحتوى والمصداقية

| البند | الحالة | الدليل |
|---|---|---|
| كل أسئلة الإنتاج مدققة | ✅ | 1549 سؤالًا: علم النفس 474 (مطابقة صف-بصف آلية 840/840 + 8 تصحيحات موثقة)، فلسفة ت1 619 (تدقيق يدوي موثق AUDIT_PHILOSOPHY.md)، فلسفة ت2 254 + منطق ت2 202 (تدقيق يدوي موثق) |
| كل المفاتيح متحققة | ✅* | *باستثناء الموضح في «حدود المصادر»: مفاتيح منطق ت2 تحققت من المصدر الثانوي + منهجية موثقة، و6 أسئلة غامضة **استُبعدت** بدل التخمين |
| مطابقة المنهج متحققة | ✅ | ت1 من كتاب الشرح الرسمي؛ ت2 من عناوين كتاب الأسئلة الرسمي — مفحوصة آليًا في validate-banks |
| مراجعة شامل علم النفس | ✅ | PSY-FULL-COMP ‏60 سؤالًا (10/وحدة) بأسئلة موثقة فقط |
| مراجعة فلسفة ت1 | ✅ | 31 امتحانًا بالهيكل الرسمي الكامل |
| مراجعة فلسفة ت2 | ✅ | 21 امتحانًا: فلسفة (4 تدريبات × 3 نماذج) + منطق كامل (7 تدريبات + شامل وحدة) + شامل ترم 40 سؤالًا |
| لا محتوى مخترع | ✅ | 100% من الأسئلة من كتب الأسئلة الرسمية، عدا 89 سؤالًا «مؤلَّف من المنهج» موسومة `authorCreated` ومصدرها موثق (25 فلسفة ت2 + 64 ت1 + 5 منطق ت2 لمواقف اضطرارية موثقة) |
| لا بيانات وهمية | ✅ | لا إحصاءات/شهادات/أعداد طلاب مزيفة — مفحوص آليًا؛ الحسابات كلها من بيانات فعلية |
| لا بيانات حساسة مكشوفة | ✅ | grep نظيف للكود (لا كلمات مرور/بريد المسؤول/مفاتيح) — مفحوص آليًا؛ الهاتف الخاص بالمعلم لا يظهر في الملف العام (اختبار) |
| مراجعة اللغة العربية | ✅ | مصطلحات موحدة مفحوصة آليًا (تسليم الامتحان/مراجعة الإجابات/إجابتك/الإجابة الصحيحة/إجابة صحيحة/إجابة خاطئة/الموضوع/الدرس/الوحدة/الترم/الامتحان الشامل)؛ لا نصوص إنجليزية في واجهة الطالب |

### التشغيل والأداء

| البند | الحالة | الدليل |
|---|---|---|
| أندرويد/هواوي/آيفون/سطح المكتب | ✅* | تصميم mobile-first RTL بفواصل 860/600px، بدون تمرير أفقي (لا عناصر بعرض ثابت أكبر من الشاشة)، أزرار ≥44px — *التحقق البكسلي المباشر تعذر في بيئة التطوير (انظر أدناه) — المعاينة الحية متاحة للمراجعة البصرية النهائية |
| خفة الأداء | ✅ | واجهة الطالب 84KB غير مضغوطة بدون أي أطر؛ بنك الأسئلة ~209KB مضغوطة (ضمن حد 1MB المجاني)؛ ترويسات تخزين مؤقت للملفات الثابتة؛ لا استقصاء/polling — مفحوص آليًا |
| متطلبات Cloudflare المجانية محفوظة | ✅ | Worker واحد + KV واحد + Assets؛ لا Workers مدفوعة؛ الحزمة ضمن الحدود |
| كلمة المرور/الأسرار غير مكشوفة | ✅ | الإعداد الأولي عبر الشاشة أو أسرار wrangler فقط؛ كلمة المرور PBKDF2-120k على الخادم؛ تغييرها من لوحة التحكم (مختبر آليًا) |

## ثالثًا: كيف أُجري الفحص البصري (بشفافية)

بيئة التطوير لا تتضمن متصفحًا رسوميًا وتمنع الشبكة تحميل Chrome (حُاول عبر puppeteer وapt ومصادر بديلة). لذلك أُجري **فحص جودة على مستوى الـDOM بشاشات حقيقية**: تشغيل app.js الفعلي داخل jsdom مع الكتالوج الحقيقي من الخادم، والتحقق من كل شاشة رئيسية (الرئيسية/المواد/الوحدات/الفصول/بدء الامتحان/شاشة الأسئلة/المراجعة/النتيجة/لوحة التحكم) — 60 فحصًا آليًا **كلها ناجحة** (`node tools/ui-qa.mjs`): بنية العناصر، تغطية كل فئات CSS المستخدمة، الأرقام الفعلية، منع التسليم الناقص، النتيجة، سلامة اللغة، الخفة. **المعاينة الحية متاحة لاعتماد بصري نهائي من صاحب المنصة قبل النشر.**

## رابعًا: أوامر إعادة الإنتاج

```bash
cd cloudflare
npm run build:banks   # بناء banks.json من ملفات .gs
npm run validate      # 60+ فحص بيانات (مطابقة المصادر/البنية/التوازن/التسريب)
npm test              # 69 اختبار خادم (دورة درجة كاملة × 83 امتحانًا، أمان، CSRF، منع التسليم الناقص…)
npm run dev           # المعاينة (يتطلب تشغيل الخادم قبل qa)
npm run qa            # 60 فحص واجهة (يتطلب: npm i jsdom في مجلد مؤقت — موثق داخل الأداة)
```

---

# تحديث المرجع المعتمد (الجولة 5) — إعادة تصميم هيكلية + إصلاح خلل التنقل

> بتاريخ ٩ سبتمبر 2026 اعتمد المالك صورة مرجعية نهائية (أزرق/ذهبي، فاتح) مع توجيه من ٢٠ قسمًا.
> هذا القسم يوثق تنفيذها بندًا بندًا. التحقق: **اختبارات خادم 69/69 + فحص واجهة 69/69 (يعمل بالنقرات الحقيقية وحدث hashchange داخل jsdom) + تحقق بيانات كامل (لا تغيير في البيانات: 1549 سؤالًا / 83 امتحانًا).**

## إصلاح الخلل الجوهري (§19): عودة الطالب للرئيسية بعد إدخال البيانات

**السبب الجذري (شُخّص بالكود وأُثبت بالاختبار):** دالة `route()` لم يكن لها فرع للهاش `#/quiz` — بعد نجاح `startExam()` كان `go('#/quiz')` يطلق حدث `hashchange` (غير متزامن) فتسقط الدالة في الفرع الافتراضي `renderHome()` فتعود الرئيسية فوق شاشة الامتحان مباشرة. الخلل كان يحدث في المتصفح الحقيقي ١٠٠٪ من المرات، ولم يلتقطه فحص الجولة السابقة لأنه كان يشغّل الدوال مباشرة بدلًا من النقر على زر «ابدأ الامتحان» الفعلي.

**الإصلاح:** فروع صريحة في `route()` لكل مسار (`start` / `quiz` / `result`) + `go()` يوجّه يدويًا عند تطابق الهاش + التسليم ينقل إلى `#/result`. **اختبار الانحدار** في `tools/ui-qa.mjs` القسم [4]: تدفق كامل بالنقرات الحقيقية (الرئيسية ← بيانات الطالب ← الصف ← الموضوع ← بدء الامتحان ← أسئلة ← تسليم ← نتيجة ← مراجعة) مع التأكيد صراحةً أن شاشة الأسئلة تبقى بعد البدء.

## تنفيذ أقسام التوجيه (٢٠ قسمًا)

| § | المطلوب | الحالة | الدليل |
|---|---|---|---|
| 1 | وضع فاتح فقط — أزرق احترافي + ذهبي + لافندر، ظلال ناعمة، بطاقات دائرية | ✅ | `styles.css`: `--primary:#1E56C8`، `--gold:#C99A2E`، خلفية `#F5F7FD` بتدرجات شعاعية ناعمة؛ لا `prefers-color-scheme: dark` ولا مبدّل |
| 2 | Hero: صورة معلم كبيرة يمينًا بعناصر زخرفية + عنوان يسارًا + بطاقتا الصفين | ✅ | `grid-template-areas: "media body" / "media grades"` (الصورة يمينًا في RTL)، صورة ٢٩٦px بإطار أبيض وblob/ring/شارة ذهبية/شارة «تصحيح فوري» عائمة (CSS فقط)، عنوان «اختبر نفسك وقيّم مستواك!»، بطاقتا صف مصغرتان تحت الأزرار |
| 3 | هيدر: هوية يمينًا + تنقل وسطًا + أيقونات تواصل يسارًا (فعلية فقط) | ✅ | `mainnav`: الرئيسية/الامتحانات/عن المنصة + «تواصل معنا» وأيقونات التواصل **تظهر فقط عند وجود روابط فعلية** — لا روابط مخترعة (مُتحقق: مخفية حاليًا لعدم إعداد روابط) |
| 4 | لا عناصر مزيفة (إحصاءات/شهادات/تقييمات/أرقام) | ✅ | كل الأرقام محسوبة من الفهرس (٦ وحدات/٢٤ موضوعًا/٣١+٥٢ امتحانًا)؛ لا يوجد أي رقم ثابت |
| 5 | بطاقتا الصف: الأول الثانوي (فلسفة ومنطق) / الثاني الثانوي (بكالوريا — علم النفس) والاختيار للصف أولًا | ✅ | بطاقات كبيرة بقسم «اختر صفك للبدء» + مصغرتان في الـHero، بوصف وإحصاءات فعلية وزر «ابدأ الآن» |
| 6 | المسار: ابدأ ← بيانات الطالب (اسم/هاتف/صف) ← المادة ← الترم/الوحدة ← الموضوع ← الامتحان | ✅ | شاشة `#/start` جديدة: الاسم الكامل + الهاتف + اختيار الصف (بطاقتان قابلتان للتحديد) ← منهج الصف. اختيار الصف لا يمسح البيانات المُدخلة، وبطاقة الصف تُفعّل الصف مسبقًا |
| 7 | تنظيم أكاديمي لا قائمة مسطحة | ✅ | علم النفس: وحدات ← موضوعات ← شوامل. الفلسفة: ترم (تبويبان) ← وحدات ← فصول ← موضوعات ← شوامل وحدة/ترم. بطاقات وحدات برأس مرقّم |
| 8 | رقم الموضوع + اسم الدرس إلزامي حرفيًا | ✅ | كل صف موضوع: فقاعة «الموضوع N» + اسم الدرس حرفيًا من الفهرس + «امتحان تدريبي — نموذج N — ٢٠ سؤالًا». لا «امتحان 1» مجردًا في أي مكان |
| 9 | بطاقات امتحانات مصقولة + شارة ⭐ مميزة للشوامل | ✅ | بطاقات بيضاء بحدود ناعمة وزوايا دائرية وظلال خفيفة + سهم CTA؛ الشوامل بتدرج ذهبي وشارة «⭐ امتحان شامل» + قسم إبراز نهائي للشامل الكبير |
| 10 | واجهة الامتحان بنفس اللغة البصرية | ✅ | خلفية فاتحة، بطاقة سؤال بيضاء بنص كبير، خيارات مريحة بحالة تحديد زرقاء واضحة، شريط تقدم، «السؤال X من Y»، السابق/التالي، لاصق علوي |
| 11 | منع التسليم الناقص (أمامي + خلفي) مع تحديد الأسئلة | ✅ | الواجهة: زر معطّل + «ناقص 1» + سرد أرقام الأسئلة + انتقال إليها. الخادم: 400 مع مصفوفة `unanswered` (مختبر: رفض فعلي مع رقم السؤال) |
| 12 | النتيجة: درجة/نسبة/صحيح/خاطئ + مراجعة كاملة بإجابتك والإجابة الصحيحة | ✅ | حلقة نتيجة + شارات + مراجعة لكل سؤال ✅/❌ بـ«إجابتك» و«الإجابة الصحيحة» + فلاتر |
| 13 | شريط مميزات بقدرات حقيقية | ✅ | 📚 امتحانات منظمة / 📊 نتيجتك فورًا / 📝 مراجعة الإجابات — ثلاث قدرات فعلية فقط |
| 14 | قسم واضح عن المعلم ببيانات فعلية + صورة + تواصل | ✅ | «عن المعلم والمنصة»: الاسم/التخصص/النبذة/العام الدراسي من بيانات المعلم الفعلية + صورة + روابط التواصل عند إعدادها |
| 15 | استجابة مقصودة للموبايل (تكديس ذكي، صورة بارزة، أزرار بعرض كامل) | ✅ | `@media 920/760/600px`: الHero يتكدس نص→صورة→بطاقات، التنقل صف قابل للتمرير، الأزرار بعرض كامل، لا تمرير أفقي (تصميم mobile-first) |
| 16 | أداء خفيف | ✅ | 107KB غير مضغوطة لكل ملفات الطالب+اللوحة (بلا أطر)، CSS فقط للزخارف، لا فيديو/مكتبات/استقصاء، ترويسات تخزين مؤقت |
| 17 | المرجع ≠ محتوى مزيف — بيانات فعلية فقط | ✅ | الاسم/البيانات من سجل المعلم الفعلي، الأرقام من الفهرس، الروابط عند إعدادها فقط، الصورة الحقيقية عند رفعها (حالة فارغة أنيقة الآن) |
| 18 | اختبار قبول بصري مقابل المرجع | ✅/⚠️ | البنية والتكوين والهوية متطابقة مع التوجيه (موثق أعلاه)؛ الفحص على مستوى DOM (لا متصفح رسومي في بيئة التطوير) — **المعاينة الحية متاحة للاعتماد البصري النهائي من المالك** |
| 19 | إصلاح خلل العودة للرئيسية + اختبار المسار كاملًا | ✅ | السبب الجذري والإصلاح موثقان أعلاه؛ اختبار انحدار دائم بالنقرات الحقيقية (ui-qa [4]) |
| 20 | سلامة البيانات (لا حذف/تصفير/دفع قسري) | ✅ | لا عملية هجرة ولا مسّ للبيانات: `npm run validate` قبل وبعد — 1549 سؤالًا/83 امتحانًا كما هي؛ `git push` عادي للفرع |

## ملاحظتان للمالك قبل النشر

1. **صورة المعلم الحقيقية والروابط الاجتماعية**: غير مُعدّة حاليًا — تُرفع/تُضبط من لوحة التحكم (المعلمون ← تحرير ← الصورة/روابط التواصل) وتظهر فورًا في الهيدر والHero وقسمي «عن المعلم» و«تواصل معنا».
2. **ألوان المعلم في الإنتاج**: إن كانت هناك نسخة قديمة من سجل المعلم محفوظة في KV الإنتاج بألوان خضراء فستتجاوز الأزرق الافتراضي — تُعدَّل من لوحة التحكم (اللون الأساسي `#1E56C8` والذهبي `#C99A2E`) أو تُحذف نسخة KV لتعود الافتراضية.

---

# الجولة 6 — تجميد التصميم + نماذج الامتحان + فحص شامل (10 سبتمبر 2026)

> **التصميم المعتمد مُجمَّد** — لم يُغيَّر فيه أي شيء (ألوان/هيرو/بطاقات/هيدر/خطوط/تركيب). التغيير الوحيد: طريقة عرض **نماذج الامتحان** داخل صف الموضوع حسب طلب المالك.

## 1) تغيير نماذج الامتحان (البند 1 المطلوب)

- **قبل:** رقائق صغيرة «نموذج 1/2/3» تبدو كوسوم لا كأزرار امتحانات.
- **بعد:** منطقة مجمّعة **«الامتحانات المتاحة»** داخل نفس صف الموضوع (بدون أي تغيير في تصميم البطاقة): كل نموذج **زر امتحان مستقل** يعرض «نموذج N» + «امتحان تدريبي · 20 سؤالًا» (العدد من البيانات الفعلية)، بارتفاع لمس ≥ 58px، مع حالة hover وضغط واضحة، وشبكة تلتف لعمود واحد على الموبايل.
- **البيانات:** 13 درسًا متعدد النماذج (12 بثلاثة نماذج + 1 بأربعة) — كلها في الفلسفة؛ **صفر أسئلة مشتركة بين نماذج نفس الدرس** (نماذج مستقلة فعلًا).

## 2) مسار كل نموذج (البند 2) — مُختبر بالنقرات الحقيقية

اختبار انحدار دائم في `tools/ui-qa.mjs` [4ب]: «نموذج 2» يفتح `#/e/PHI-C1-T1-2` تحديدًا (العنوان والأسئلة ومفرد الجلسة كلها للنموذج الثاني)، و«نموذج 4» يفتح امتحانًا مختلفًا — لا توجيه كل النماذج لنفس الامتحان. النموذج 2 مُسلَّم بالكامل حتى النتيجة مع تحقق تصحيح متسق ذاتيًا لكل سؤال.

## 3) نتائج الفحص الشامل

| الفحص | النتيجة |
|---|---|
| فحص الواجهة (نقرات حقيقية، 12 قسمًا) | **89/89 ✓** |
| اختبارات الخادم | **69/69 ✓** |
| تحقق البيانات الكامل | **PASSED ✓** (1549 سؤالًا / 83 امتحانًا مطابقة للمصادر بايت-ببايت) |
| سلامة البيانات قبل/بعد | banks.json ببصمة md5 `75f40a0e…` — **لم تتغير** |
| نسخة احتياطية قبل العمل | فرع محلي `backup/pre-models-qa-20260910` عند `4208cba` |

### فحوص أُضيفت في هذه الجولة (داخل ui-qa)
- **الأمان [7ب]:** مسارات الإدارة ترفض بدون جلسة (401)؛ التوكن المزوَّر مرفوض (403)؛ لا كلمات مرور/مفاتيح/مفاتيح إجابة في app.js؛ حقول سؤال الجلسة = نص/خيارات فقط (مفتاح الإجابة لا يُرسل قبل التسليم).
- **ملف المعلم [7ج]:** عند إعداد روابط فعلية تظهر أيقونات الهيدر الثلاث + «تواصل معنا» + قسم التواصل بروابطها الصحيحة؛ بدون روابط لا يظهر أي شيء وهمي (الحالة الفعلية الآن).
- **التصحيح [4ب]:** isCorrect ≡ (إجابتك = الإجابة الصحيحة) لكل سؤال؛ النسبة = round(الدرجة/العدد×100)؛ صحيح+خطأ=المجموع.

### ملاحظات صادقة (كالعادة)
- **لا متصفح رسوميًا في بيئة التطوير**: لا لقطات شاشة ولا اختبار أجهزة حقيقية (Android/Huawei/iPhone). البديل: فحص DOM بالنقرات الفعلية + تحققات CSS (أحجام لمس ≥48px، شبكة تلتف، RTL سليم) — الاعتماد البصري النهائي للمالك عبر المعاينة.
- **تدقيق مفاتيح الإجابة مقابل المصادر** أُنجز في الجولات السابقة (840/840 نفس + 619/619 + 254/254 + 202/202 مطابقة للمصادر، 6 أسئلة غامضة مستبعدة موثقة، 4 حالات تحفظ مفتوحة تحتاج كتاب شرح ت2) — لا تغيير في أي محتوى تعليمي هذه الجولة، ولا مفاتيح مخمَّنة.
- **تكرارات موثقة موروثة من المصدر:** U2-T1 (6 أسئلة مكررة داخل الامتحان) وU2-COMPREHENSIVE (3) — موروثة حرفيًا من كتاب المصدر الرسمي (مطابقة 840/840 تشملها)؛ تُركت كما هي التزامًا بعدم تغيير المحتوى التعليمي.

## 4) إغلاق مرحلة Multi-Teacher + Production QA — إصلاح عيبين من اختبار العزل (سبتمبر ٢٠٢٦)

> **ملاحظة توثيق:** الـ commit الأصلي الذي حمل هذه الإصلاحات (`e332238`) فُقد قبل دفعه (لم يُعثر عليه في GitHub ولا في أي مستودع محلي). أُعيد إنشاء **نفس التغييرات الموصوفة** في commit جديد موثق بذلك، دون أي تعديل على البيانات أو التصميم المعتمد.

### العيب الأول: تلوث الغلاف الثابت ببيانات المعلم الافتراضي (Static Teacher Contamination)
- **قبل:** الشل الثابت `index.html` كان يحمل «د. مصطفى تيتو» في `title` + `meta description` + هوية الشريط العلوي (`brandName/brandSub/brandLogo`) + `footer` — فيظهر اسمه لومضةً على `/ahmed` و`/mohamed` قبل تحميل بيانات المعلم الفعلي.
- **بعد:** غلاف ثابت محايد تمامًا (العنوان «منصة الامتحانات»، وصف meta عام، هوية عامة) + `app.js` يحدّث `title/meta/الهوية/footer` **ديناميكيًا من بيانات المعلم الحالي حسب الـslug** (`renderBrand`) — لا أي اسم معلم ثابت في الغلاف.

### العيب الثاني: تباين ألوان المعلمين — توحيد الهوية البصرية
- حذف نظام `colors` المخصص للمعلمين بالكامل: من سجلات المعلمين (`worker.js` DEFAULT_TEACHERS)، من الـAPI العام (`teacherPublic`)، من مسار الحفظ (`sanitizeTeacher` — أي قيم colors واردة تُتجاهل ولا تُخزَّن)، ومن لوحة التحكم (`admin.js`: حذف color picker وقيم النموذج الافتراضية).
- جميع المعلمين على الهوية المعتمدة الواحدة: Primary `#1E56C8` / Gold `#C99A2E` — حُذف الـfallback القديم `#123B40`/`#C9A86A` والقيمة الخضراء `#0E7A5F` من نموذج إضافة المعلم.
- `styles.css`: تعليق توثيقي فقط (الهوية موحدة للجميع) — **صفر تغيير** في الـlayout أو typography أو spacing أو قيم الألوان المعتمدة.

### نتيجة إعادة الفحص الكاملة بعد الإصلاحين
| الفحص | النتيجة |
|---|---|
| فحص الواجهة (ui-qa، نقرات حقيقية) | **89/89 ✓** |
| اختبارات الخادم (run-all) | **69/69 ✓** |
| تحقق البيانات الكامل (validate-banks) | **PASS ✓** — 1549 سؤالًا / 83 امتحانًا |
| banks.json | md5 `75f40a0e…` — **لم يتغير** (لا أسئلة/إجابات/معرفات امتحانات) |
| عزل المعلمين `/ahmed` `/mohamed` | الشل المُقدَّم بلا أي بيانات ثابتة لمصطفى، والملف العام بلا حقول colors |
| أخطاء Runtime/Console | **0** |
| الأصول المحلية (styles/app/favicon/admin) | **كلها 200** |
