# PHASE FINAL — ExamManasa Production Audit

> جلسة الإغلاق النهائية. كل رقم هنا مُعاد حسابه من `cloudflare/src/data/banks.json` ومن تشغيل فعلي للاختبارات على آخر كود في الفرع `arena/01a08d7b-exammanasa` (فوق PR #6 `4916d53`). لا شيء منقول من تقارير سابقة دون إعادة تحقق.

## 1. نطاق إعادة التدقيق
- الواجهة الأمامية (`cloudflare/public/app.js`, `admin.js`, `index.html`, `admin.html`, `styles.css`) — قُرئت بالكامل.
- الخادم (`cloudflare/src/worker.js`) — قُرئ بالكامل: catalog، ملف المعلم، بدء/تسليم الامتحان، لوحة الإدارة، Durable Object.
- البيانات: `banks.json` (v5)، ملفات JSON المصدرية للترمين، `data/key-decisions.json` (140 قرارًا)، ملفات docx المصدرية (استُخرجت نصيًا للمطابقة).
- أدوات البناء/التحقق: `build-banks.mjs`, `philo-trainings.mjs`, `validate-banks.mjs`, `ui-qa.mjs`.
- الإعداد السحابي: `wrangler.jsonc` (Assets + KV + DO SQLite migration v1)، `wrangler deploy --dry-run`.

## 2. المحتوى — الشجرة
المادة → الترم → القسم (الفلسفة / المنطق) → موضوعان بالضبط → الدرس (باسمه الحقيقي «الدرس N — الاسم») → التدريب → الامتحان.

| الترم | القسم | الموضوعات | الدروس | التدريبات | الأسئلة |
|---|---|---|---|---|---|
| 1 | الفلسفة | 2 (التفكير الإنساني / الفلسفة وطبيعة الموقف الفلسفي) | 5 + 5 | 15 | — |
| 1 | المنطق | 2 (مبادئ المنطق / الاستدلال) | 8 + 1 | 15 | — |
| 1 | المجموع | 4 | 19 | 30 | 710 |
| 2 | الفلسفة | 2 (البيئية والبيوطبية / المهنية والقيم) | 3 + 2 | 5 | — |
| 2 | المنطق | 2 (الاستقراء / الاستنباط) | 3 + 3 | 9 | — |
| 2 | المجموع | 4 | 11 | 14 | 319 |

- درسا الترم الأول المستقلان: `T1-PH-RELIGION-01` (الفلسفة والدين، 15 سؤالًا) و`T1-PH-SCIENCE-01` (الفلسفة والعلم، 20 سؤالًا؛ Q50 مستبعدة بقرار موثق).
- الامتحانات الشاملة (5) في قسم منفصل عن التدريبات: `PHI-COMP`(20) `LOG-COMP`(20) `PHLO-COMP`(20) `T2L-COMP`(20) `T2-TERM-COMP`(40). النماذج القديمة (47) محفوظة في البنك، مخفية عن الفهرس، وما زالت قابلة للتصحيح خادميًا (روابط قديمة).
- إجمالي البنك: **127 امتحانًا** (علم النفس 31 — فلسفة ت1 61 — فلسفة ت2 35)، **80 معروضًا** للطالب، **1887 سؤالًا فريدًا**؛ الأسئلة الفريدة داخل التدريبات الـ44: **1029**.

## 3. الأسئلة — التغطية المصدرية (مُعاد التحقق من banks.json)
- `T1-LG-01` 20 (المصدر 1–20)، `T1-LG-02` 27 (1–27)، `T1-LG-04` 28 (1–28)، `T1-LG-05` 29 (1–30 عدا Q22: مفتاح الكتاب لا يطابق أي خيار)، `T1-LG-06` 33 (1–33)، `T1-LG-07` ×3 (20 + 21–40 + 41–75 وتشمل Q53/Q56)، `T1-PH-08` ×2 (1–20 + 21–54 عدا Q23 غير المحسومة).
- الترم الثاني: `T2-PH-BIO-01` 21 (20 + Q33 حرفية)، `T2-PH-PRO-T2` 20 (21–40: 18 حرفية + Q24/Q27 موثقتان)، `T2-LG-MODERN` 58، `T2-LG-DED-02` 20، `T2-LG-AI` 20 (Q32 ضمنها)، `T2-LG-DED-01` 20 = 15 أصلية + 5 TOPUP من DED-02 (`top_up_from_related_training` في JSON؛ مسجلة في AUDIT_PHILOSOPHY_TRAININGS.md §الأسئلة المشتركة).
- **تصحيح صياغة تقرير سابق**: PHASE_2B_FINAL_COVERAGE.md:133 يذكر «LG-01 Q21». الواقع في JSON وBank: تدريب LG-01 يضم 1–20 من مجمع 24؛ لا يوجد سؤال Q21 لـ LG-01 في مجموعة البيانات، ولا يمكن إضافته بدون مفتاح مصدري موثق (انظر §4). الحكم: **ليس عيبًا في البنك** بل خطأ صياغة في التقرير القديم.
- التكرارات داخل التدريبات المعروضة: 6 مجموعات فقط، كلها بنفس المفتاح — 5 منها TOPUP DED-01↔DED-02 (موثقة أعلاه) وواحدة (`PLT2L-019` في الامتحان الشامل ↔ `T2-LG-IND-01-Q19` في التدريب: نفس السؤال بفرق تشكيل «كلي/كلى»، نفس الإجابة A) — مقبولة (شامل ≠ تدريب).
- `T1-PH-04-Q16`: الخيارات [أثينا، أسبرطة، اليونان القديم، الشرق القديم]، المفتاح **D**. ✓
- لا `REVIEW`/`UNRESOLVED`/`needs-review` في أي سؤال معروض (0 من 1887؛ `verificationStatus` = verified للكل).
- لا مستويات صعوبة في الفهرس (`/api/catalog` يحوي 0 تكرار لـ"difficulty")، ولا في بيانات الجلسة، ولا في `app.js`/`index.html`. تظهر فقط في بنك المسؤول (`admin.js`) وهو غير متاح للطلاب.

## 4. مفاتيح الإجابة
توزيع مصدر المفتاح للأسئلة الـ1029 داخل التدريبات:

| الحالة | العدد | المعنى |
|---|---|---|
| bank-audited | 584 | مفتاح البنك المدقق يدويًا (AUDIT_PHILOSOPHY*.md) ومطابق لـ JSON |
| json-source | 268 | جديد من JSON؛ نص مفتاح الكتاب يطابق الخيار حرفيًا/قريبًا |
| official-source-decision | 138 | قرار بشري بمصدر رسمي (`data/key-decisions.json`) |
| json-source-corroborated | 20 | كما سبق + مؤكد بسؤال مشابه في البنك |
| bank-key-kept-json-unverified | 19 | تعارض؛ مفتاح JSON آلي ضعيف → احتُفظ بمفتاح البنك المدقق (موثق) |

- `STRICT_KEYS=1 validate-banks` **PASS**: 0 `needs-review`، 0 `conflict-bank-key-kept`.
- **نتيجة الفحص المصدري (docx)**: ملفات docx **لا تحوي نموذج إجابة رسميًا**؛ كتلة «نموذج الإجابة» في ذيل الملفين مولَّدة بالذكاء الاصطناعي (تبدأ بـ«بناءً على طلبك…» وتحوي بدائل «أو …»). لذلك لم تُستخدم كمصدر لأي مفتاح، وتُعامل مقارنات BOOK_KEY_AUDIT.md المبنية عليها كغير حاسمة. **لم يُغيَّر أي مفتاح في هذه الجلسة**.
- أسئلة مجمع «الفلسفة والعلم» (14 سؤالًا: PL-0180/0182/0184/0186/0188/0200/0204/0234/0238/0240/0252/0256/0266/0270) غير مدرجة في `T1-PH-SCIENCE-01`؛ مفاتيحها من التدقيق اليدوي القديم فقط ولا يوجد لها قرار مصدري (عدا PL-0200 التي تبقى في PHI-COMP). **القرار**: تبقى في البنك (النماذج القديمة/الشامل) ولا تُضاف للتدريب بدون مفتاح مصدري — لا اختلاق ولا تخمين.
- العدّاد «needsReview» في نظرة المسؤول العامة (78) هو إحصاء تدقيقي للمفاتيح غير المؤكدة من JSON التي **احتُفظ بمفتاح البنك المدقق مكانها**؛ أُعيدت تسميته في اللوحة لتوضيح ذلك. لا يصل أي منها للطالب كـ«يحتاج مراجعة».

## 5. علم النفس
sha256 للقطة (catalog + exams + examDefs + questions) = `8851437be88724537693cbf1ad57c0531e36eca4b55dcea102f7c077b44a8198` — **مطابق تمامًا** قبل الجلسة وبعدها (مُختبَر في `npm test` أيضًا). إعادة تشغيل `build-banks` تنتج ملفًا مطابقًا بايتًا عدا `generatedAt`.

## 6. تدفق الطالب
تسجيل بالاسم + الهاتف (توحيد الأرقام المصرية: `+20`/`0020`/`20`/أرقام عربية-هندية → `01XXXXXXXXX`، رفض غير الصالح)، بدء الجلسة (توكن HMAC 6 ساعات / 72 ساعة أوفلاين)، أسئلة بلا مفاتيح (حقول `id,no,options,text` فقط)، خلط بذري لكل جلسة، تسليم يتطلب إجابة كل الأسئلة، **التصحيح على الخادم فقط**، منع إعادة التسليم (نفس النتيجة تُعاد)، مراجعة الإجابات بعد التسليم. كل ذلك مُختبَر في `[4][5][6][7]`.

## 7. المعلمون
CRUD كامل من اللوحة: الاسم، الهاتف الخاص (لا يظهر للطلاب)، التخصص، النبذة، واتساب/فيسبوك/تيك توك (https فقط)، صورة (مضغوطة ≤2.5MB)، طلب الهاتف، مفعّل/معطّل، حدود المحاولات، وضع الأوفلاين، **حد الطلاب**. ID (`t_xxxx`) وslug ورابط عام تُولَّد تلقائيًا. لوحة المعلم بعدّادات حقيقية من KV/DO (نتائج، طلاب، متوسط، نسبة نجاح، حسب الامتحان، أحدث النتائج، Limit/Current/Remaining).
- **الحذف غير تدميري (جديد)**: DELETE ينقل المعلم إلى `teachers:archived` (قابل للاستعادة من اللوحة، يعود معطّلًا)، ولا يمس `results:teacher:<slug>` ولا سجل الطلاب. المعلم الافتراضي لا يُحذف (400) بل يُعطَّل فقط.
- العزل: فهرس نتائج لكل slug؛ معلم آخر لا يرى نتائج الأول (مُختبَر). البنك والامتحانات مشتركة للجميع (لا بنوك منفصلة).
- المعلم المعطّل: 404 للملف العام، 403 عند بدء الجلسة **وعند التسليم** (أُضيف الفحص عند التسليم في هذه الجلسة).

## 8. حد الطلاب (جديد في هذه الجلسة)
- الحقول: `studentLimitUnlimited` (علم حقيقي، الافتراضي true) و`studentLimit` (0 = التسجيل مغلق للجدد؛ N = أقصى عدد طلاب متمايزين). «غير محدود» **ليس رقمًا كبيرًا**؛ `studentLimit=null` في الاستجابات عندما يكون العلم مفعّلًا.
- الهوية: sha256 للهاتف المُوحَّد (أو الاسم المُطبَّع عند عدم طلب الهاتف) — نفس الهاتف بأي صيغة/اسم = طالب واحد (مُختبَر).
- التنفيذ: كائن Durable Object واحد لكل معلم (`students:<slug>`) يحوي سجل الطلاب والعدّاد؛ `/students/register` معاملة ذرية: طالب مسجَّل → مسموح دائمًا؛ طالب جديد → مسموح فقط إذا `count < limit`. يُفرض عند `/api/exam/start` **وعند** `/api/exam/submit` (توكن قديم/هوية غير مسجلة لا تتجاوز الحد).
- الاختبارات `[15]`: حد 2 → 2 مقبولان والثالث 429 برسالة عربية واضحة؛ الطالب المسجَّل يدخل امتحانات أخرى بعد الامتلاء؛ رفع الحد يفتح مقعدًا؛ حد 0 يغلق التسجيل؛ غير محدود يقبل الجميع؛ **سباق: حد 500 / الحالي 499 / 6 تسجيلات متزامنة → 1 مقبول + 5 مرفوضة بالضبط، والعدّاد = 500**؛ حقن `teacherId` من العميل لا يغيّر شيئًا.
- اللوحة: بطاقة المعلم + نافذة التعديل + لوحة المعلم + النظرة العامة تعرض الحد/الحالي/المتبقي.

## 9. وضع عدم الاتصال
توكن 72 ساعة للمعلم المفعّل له الوضع (وإلا 6 ساعات)، مسودة الإجابات في sessionStorage، طابور تسليم في localStorage، إرسال تلقائي عند `online` وعند الزيارة التالية، التصحيح يبقى خادميًا. **تحسين هذه الجلسة**: انقطاع الشبكة أثناء الإرسال نفسه (أو 5xx) يُدخل التسليم في الطابور بدل رسالة خطأ ضائعة؛ الرفض النهائي (4xx) فقط يُسقط العنصر مع تنبيه. مُختبَر في `[13]` وفحص الواجهة.

## 10. الإدارة والأمان
- الجلسة: PBKDF2-SHA256 (120k)، كوكي HttpOnly+Secure+SameSite=Strict، ترويسة CSRF، تحديد معدل الدخول، حساب واحد فقط، تغيير كلمة المرور.
- لا مفاتيح في أي سطح للطالب (catalog/session/HTML) — مُختبَر آليًا على 127 امتحانًا. الدرجة المحقونة من العميل تُتجاهل. توكن متلاعب به → 403.
- لا يُوثق بالعميل: المعلم يُشتق من slug الطلب ثم يُختم داخل التوكن الموقّع؛ التسليم يستخدم `sess.slug` من التوكن لا من الجسم؛ `teacherId`/`studentId`/`score` من العميل لا أثر لها.
- **معالجة الأخطاء (جديد)**: صنف `ApiError` — رسائل الأخطاء المتوقعة فقط تصل للعميل؛ أي استثناء داخلي → `500` برسالة عامة بلا تفاصيل/stack. ترويسات الأمان على كل الاستجابات. `robots.txt` يمنع `/admin` و`/api`.
- أُزيل الكود الميت `makeToken`.

## 11. Cloudflare Free
Workers + Static Assets + KV + Durable Objects (SQLite، مشمولة في Free). لا VPS/قاعدة مدفوعة/اشتراكات. `wrangler deploy --dry-run`: حزمة 4.3MB / **355KB مضغوطة** (< 1MB). كتابات KV لكل تسليم ~3 (موثق في README مع حدود الخطة). الطلبات الدورية في الواجهة: لا شيء.

## 12. الأداء والتكامل
الفهرس 69KB مع تخزين مؤقت CDN (5 دقائق)، ملفات المعلم مخبأة 60 ثانية، الأصول الثابتة ساعة. `/api/catalog` لا يحوي نصوص أسئلة أو قوائم معرفات. البناء حتمي (نفس banks.json بايتًا).

## 13. الاختبارات (تشغيل فعلي على HEAD)
| البوابة | النتيجة |
|---|---|
| `node tools/validate-banks.mjs` | PASS (تحذير واحد موثق) |
| `STRICT_KEYS=1 node tools/validate-banks.mjs` | PASS |
| `npm test` (cloudflare) | **129/129** (كانت 111 + 18 جديدة: حد الطلاب، الأرشيف/الاستعادة) |
| `node tools/ui-qa.mjs` (jsdom، يتطلب `wrangler dev`) | **113/113** |
| `wrangler deploy --dry-run` | PASS |
| Psychology sha256 | UNCHANGED |

## 14. تغييرات هذه الجلسة (commits `final-audit:` على الفرع)
1. تعقيم أخطاء الخادم (ApiError)، إزالة `makeToken`، إصلاح تحميل jsdom في `ui-qa.mjs` (+ jsdom devDependency).
2. حد الطلاب لكل معلم (DO ذري، بدء + تسليم)، Limit/Current/Remaining في اللوحة، حذف غير تدميري + استعادة، فحص المعلم المعطّل عند التسليم، اختبارات [15].
3. طابور الأوفلاين يلتقط فشل الشبكة أثناء الإرسال و5xx؛ تحديث README (الأرقام والميزات).
4. هذا المستند.

## 15. البلوكرز والقرارات المفتوحة
- **ZERO BLOCKERS.**
- قرارات موثقة (ليست بلوكرز): Q50 (SCIENCE) وQ22 (LG-05) وQ23 (PH-08-T2) مستبعدة لأن مفتاح الكتاب لا يطابق أي خيار؛ 14 سؤالًا من مجمع «الفلسفة والعلم» تبقى خارج التدريب لغياب مفتاح مصدري؛ نموذج الإجابة في ذيل docx غير معتمد (مولَّد آليًا).
- متطلبات النشر اليدوي (خارج الكود، موثقة في cloudflare/README.md): إنشاء KV واستبدال المعرف في `wrangler.jsonc`، وضبط `SESSION_SECRET` وبيانات المسؤول الابتدائية عبر `wrangler secret`.
